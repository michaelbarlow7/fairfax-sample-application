package com.michaelbarlow.fairfaxapplication

import io.appflate.restmock.RESTMockServer

object Constants {

    val BASE_URL = RESTMockServer.getUrl()
//    val BASE_URL = RESTMockServer.getUrl().replace("https", "http")
}